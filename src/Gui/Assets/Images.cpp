#include <pch.h>

#include "Images.h"

//#include <ofMain.h>

namespace Images{

/*
	void load(){
		ofSetDataPathRoot(std::filesystem::current_path());
		static ofImage test;
		static ofImage test2;
		test.load("icons/KeyIcon.png");
		test2.load("icons/KeyIcon.png");
		bool has = test2.isUsingTexture();
		ofTexture tex = test2.getTexture();
		ofTextureData texdata = tex.getTextureData();
		keyIcon = test2.getTexture().getTextureData().textureID;
	}
	unsigned int keyIcon;
*/

}
